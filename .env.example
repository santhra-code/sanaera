# PostgreSQL connection string — replace with your own local/dev database
DATABASE_URL="postgresql://username:password@localhost:5432/sanaera?schema=public"

# NextAuth secret — generate your own with: openssl rand -base64 32
NEXTAUTH_SECRET="replace-with-generated-secret"
NEXTAUTH_URL="http://localhost:3000"